# ⚡ 立即执行数据库初始化

## 🎯 问题原因

1. **路径错误**：你在 `/home/l/美业CRM`，但文件在 `medical-beauty-crm-landing` 子目录
2. **PostgreSQL用户**：用户 "l" 在PostgreSQL中不存在

## ✅ 解决方案（3选1）

### 方案1：使用完整初始化脚本（最简单）

```bash
cd /home/l/美业CRM/medical-beauty-crm-landing
bash 完整初始化.sh
```

这个脚本会：
- ✅ 自动进入正确目录
- ✅ 创建数据库
- ✅ 创建PostgreSQL用户
- ✅ 执行初始化SQL
- ✅ 验证结果

### 方案2：手动执行（如果方案1需要sudo密码）

```bash
# 1. 进入正确目录
cd /home/l/美业CRM/medical-beauty-crm-landing

# 2. 创建数据库
sudo -u postgres psql -c "CREATE DATABASE medical_beauty_crm;"

# 3. 执行初始化
sudo -u postgres psql -d medical_beauty_crm -f scripts/init-database.sql

# 4. 验证
sudo -u postgres psql -d medical_beauty_crm -c "\dt"
```

### 方案3：使用Drizzle迁移（如果PostgreSQL用户已配置）

```bash
cd /home/l/美业CRM/medical-beauty-crm-landing

# 确保.env中的DATABASE_URL正确
# 然后运行：
npm run db:push
```

## 🔍 验证

初始化完成后，检查：

```bash
# 检查表
psql -U postgres -d medical_beauty_crm -c "\dt"

# 检查配置
psql -U postgres -d medical_beauty_crm -c "SELECT * FROM system_config;"
```

## 🎉 完成后

访问：**http://localhost:3000**
