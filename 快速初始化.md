# 🚀 快速初始化数据库

## 问题解决

你当前在 `/home/l/美业CRM` 目录，但SQL文件在 `medical-beauty-crm-landing` 目录。

## 方法1：使用初始化脚本（推荐）

```bash
cd /home/l/美业CRM/medical-beauty-crm-landing
bash init-db.sh
```

## 方法2：手动执行（如果方法1失败）

```bash
# 1. 进入正确的目录
cd /home/l/美业CRM/medical-beauty-crm-landing

# 2. 执行SQL脚本（选择以下任一方式）

# 方式A：使用sudo（需要密码）
sudo -u postgres psql -d medical_beauty_crm -f scripts/init-database.sql

# 方式B：直接连接（如果配置了peer认证）
psql -d medical_beauty_crm -f scripts/init-database.sql

# 方式C：指定用户
psql -U postgres -d medical_beauty_crm -f scripts/init-database.sql
```

## 方法3：直接在psql中执行

```bash
cd /home/l/美业CRM/medical-beauty-crm-landing

# 连接到数据库
psql -d medical_beauty_crm

# 在psql提示符下执行：
\i scripts/init-database.sql
\q
```

## 验证初始化

```bash
cd /home/l/美业CRM/medical-beauty-crm-landing

# 检查表是否创建
psql -d medical_beauty_crm -c "\dt"
```

## 如果PostgreSQL连接失败

检查PostgreSQL服务：

```bash
# 检查服务状态
sudo systemctl status postgresql

# 启动服务（如果未运行）
sudo systemctl start postgresql

# 检查PostgreSQL用户
sudo -u postgres psql -c "\du"
```

## 完成后

初始化完成后，访问：http://localhost:3000
